{
  "name": "barber-booking",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "TZ=Asia/Shanghai next dev",
    "build": "TZ=Asia/Shanghai next build",
    "start": "TZ=Asia/Shanghai next start",
    "lint": "eslint",
    "check:status": "sh -c 'rg -n \"CANCELED|CANCELLED\" lib app pages -g\"*.{ts,tsx,js,mjs,cjs}\" --glob \"!lib/status.ts\" --glob \"!lib/bookingStatus.ts\" --glob \"!eslint.config.mjs\" --glob \"!node_modules/**\" --glob \"!.next/**\" --glob \"!dist/**\"; code=$?; if [ $code -eq 0 ]; then echo \"Found forbidden status literals (use lib/status.ts constants instead).\"; exit 1; elif [ $code -eq 1 ]; then echo \"OK: no forbidden status literals found.\"; exit 0; else exit $code; fi'",
    "db:audit": "node scripts/migration-audit.mjs",
    "db:check": "npm run db:audit && git diff --quiet docs/MIGRATION_RISK_REGISTER.md",
    "db:migrate:deploy": "bash scripts/db-migrate-deploy.sh",
    "db:validate": "npx prisma validate",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "dotenv": "^17.2.3",
    "luxon": "^3.7.2",
    "next": "16.0.10",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/luxon": "^3.7.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.0.3",
    "prisma": "^6.19.0",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
