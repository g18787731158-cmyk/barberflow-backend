# Manual SQL Runbook

适用场景（必须手工 SQL）：
- 大表重写、回填、分批更新
- 在线索引、表结构重排
- 需要严格限流/观测的变更

## 必须遵守
- 幂等（重复执行无副作用）
- 带 WHERE（避免全表）
- 分批执行（LIMIT/分片）
- 限速（sleep/节流）
- 可观测（记录影响行数）

## 必须记录
- 变更原因
- 影响范围（表/字段/行数）
- 回滚方式
- 执行人/时间
- SQL 版本（commit hash）

## 模板
```
标题：
原因：
影响范围：
回滚方式：
执行人/时间：
SQL：
```

